# API keys (never commit real keys!)
# config.yaml contains real API keys and should NOT be committed
# Use config.yaml.template for version control (committed with placeholder keys)
config.yaml

.DS_Store
__pycache__/
*.pyc

# Temporary processing files
tmp/

# Personal scratch doc
thoughts.txt