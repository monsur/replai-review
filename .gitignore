# API keys (if accidentally committed)
# Note: Keep config.yaml in git but don't commit with real API keys
config.yaml

.DS_Store
__pycache__/
*.pyc

# Temporary processing files
tmp/

# Generated newsletters (but keep team_logos)
web/*.html

# Personal scratch doc
thoughts.txt